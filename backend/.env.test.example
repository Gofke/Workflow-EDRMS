# Test database. MUST be a separate database from the pilot one: the suite
# truncates tables between specs.
NODE_ENV=test
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=juspol
DB_PASSWORD=change_me
DB_DATABASE=juspol_edrms_test
SESSION_SECRET=test_only_secret
SESSION_IDLE_MINUTES=30
CORS_ORIGIN=http://localhost:5173
SEED_PASSWORD=TestOnly!2026
REGISTRATION_MANDATORY_FIELDS=subject
DOSSIER_IDENTITY_PATTERN=DOS-{YEAR}-{SEQ}
DOCUMENT_STORAGE_PATH=./test-captured-documents
